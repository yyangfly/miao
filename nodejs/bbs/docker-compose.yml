services:
  bbs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bbs-dev
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
    command: npm run dev
    volumes:
      - .:/app
      - /app/node_modules
